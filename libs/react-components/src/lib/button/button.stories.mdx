import GoAButton from './button';
import {
  Meta,
  Preview,
  Story,
  ArgsTable,
  Description,
} from '@storybook/addon-docs/blocks';

import { action } from '@storybook/addon-actions';
import { DoDont } from '@abgov/shared/storybook-common';

<Meta
  title="React/Basic Elements/Button"
  component={GoAButton}
  argTypes={{
    buttonType: {
      control: {
        type: 'select',
        options: ['primary', 'secondary', 'tertiary'],
      },
    },
    buttonSize: {
      control: {
        type: 'select',
        options: ['small', 'normal'],
      },
    },
    title: {
      control: 'text',
    },
    onClick: { action: 'You have clicked the button', control: null },
    children: {
      control: 'text',
    },
  }}
/>

# Button

A user input control that performs an action, typically on click

## Overview

export const Template = (args) => (
  <div>
    <GoAButton buttonType="primary">
      Primary
    </GoAButton>
    <GoAButton buttonType="secondary">
      Secondary
    </GoAButton>
    <GoAButton buttonType="tertiary">
      Tertiary
    </GoAButton><br /><br />
    <GoAButton {...args}>
      Demo
    </GoAButton>
  </div>
);

<Canvas>
  <Story
    name="Variants"
    args={{
      buttonSize: 'normal',
      title: 'text',
      children: 'Admin',
      onClick: (value) => {
        action('button clicked')(value);
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Examples

```html
<GoAButton buttonType="primary" buttonSize="normal" title="Hovering over">
  Click Me!
</GoAButton>
```

## Properties

<Description of={GoAButton} />
<ArgsTable of={GoAButton} />
