import GoACallout from './callout';
import {
  Meta,
  Preview,
  Story,
  ArgsTable,
  Description,
} from '@storybook/addon-docs';

import { action } from '@storybook/addon-actions';
import { DoDont } from '@abgov/shared/storybook-common';
const content = (
  <div>
    Information to the user goes in the content. Information can include{' '}
    <b>markup</b> as desired.
  </div>
);

<Meta
  title="React/Components/Callout"
  component={GoACallout}
  argTypes={{
    type: {
      control: {
        type: 'select',
        options: ['important', 'information', 'event', 'emergency', 'success'],
      },
    },
    title: {
      control: {
        type: 'text',
      },
    },
    content: {
      control: {
        type: 'text',
      },
    },
  }}
/>

# Callouts

Important changes or facts given a strong visual emphasis so that users take
notice and read the information.

## Overview

export const Template = (args) => <GoACallout {...args}>{content}</GoACallout>;

<Canvas>
  <Story
    name="Variants"
    height={200}
    args={{
      type: 'information',
      title: 'Callout Title',
      content: '',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Examples

```html
<GoACallout
  type="'important || 'information' || 'event' || 'emergency' || 'success'"
  title="Callout Title"
  content="Information to the user goes in the content"
  <
/>
```

or

```html
<GoACallout type="information" , title="Callout Title">
  Information to the user goes in the <i>content</i>
</GoACallout>
```

## Properties

<Description of={GoACallout} />
<ArgsTable of={GoACallout} />
