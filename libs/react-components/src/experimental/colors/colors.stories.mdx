import {
  Meta,
  Preview,
  Story,
  ArgsTable,
  Description,
} from '@storybook/addon-docs';

import { GoAIconButton, GoABadge, GoAButton } from '..';

import { action } from '@storybook/addon-actions';
import { Dont } from '@abgov/shared/storybook-common';

<Meta title="Design/Colors" argTypes={{}} />

export const Template = (args) => {
  const times = [1, 2, 3, 4, 5, 6, 7, 8, 9];
  return (
    <div style={{ display: 'flex', alignContent: 'center', fontSize: '12px' }}>
      {times.map((_, i) => {
        return (
          <div
            key={i}
            style={{
              padding: '1rem',
              flex: '1 1 100%',
              backgroundColor: `var(--color-${args.color}-${i + 1}00)`,
              textAlign: 'center',
              fontWeight: 'bold',
            }}
          >
            {`${args.color}-${i + 1}00`}
          </div>
        );
      })}
    </div>
  );
};

export const ListWithTertiaryButtonTemplate = (args) => {
  return (
    <table style={{ width: '100%' }}>
      <tr>
        <td>Jim</td>
        <td>Smith</td>
        <td>Edmonton</td>
        <td>
          <GoAButton onClick={() => {}} size="medium" type="secondary">
            Delete
          </GoAButton>
          <GoAButton onClick={() => {}} size="medium" type="secondary">
            View Profile
          </GoAButton>
        </td>
      </tr>
      <tr>
        <td>Sally</td>
        <td>Johnson</td>
        <td>Calgary</td>
        <td>
          <GoAButton onClick={() => {}} size="medium" type="secondary">
            Delete
          </GoAButton>
          <GoAButton onClick={() => {}} size="medium" type="secondary">
            View Profile
          </GoAButton>
        </td>
      </tr>
      <tr>
        <td>Will</td>
        <td>Green</td>
        <td>Vancouver</td>
        <td>
          <GoAButton onClick={() => {}} size="medium" type="secondary">
            Delete
          </GoAButton>
          <GoAButton onClick={() => {}} size="medium" type="secondary">
            View Profile
          </GoAButton>
        </td>
      </tr>
    </table>
  );
};

export const ListWithBorderlessButtonTemplate = (args) => {
  return (
    <table style={{ width: '100%' }}>
      <tr>
        <td>Jim</td>
        <td>Smith</td>
        <td>Edmonton</td>
        <td>
          <GoAButton
            onClick={() => {}}
            size="medium"
            variant="danger"
            type="borderless"
          >
            Delete
          </GoAButton>
          <GoAButton onClick={() => {}} size="medium" type="borderless">
            Show
          </GoAButton>
        </td>
      </tr>
      <tr>
        <td>Sally</td>
        <td>Johnson</td>
        <td>Calgary</td>
        <td>
          <GoAButton
            onClick={() => {}}
            size="medium"
            variant="danger"
            type="borderless"
          >
            Delete
          </GoAButton>
          <GoAButton onClick={() => {}} size="medium" type="borderless">
            Show
          </GoAButton>
        </td>
      </tr>
      <tr>
        <td>Will</td>
        <td>Green</td>
        <td>Vancouver</td>
        <td>
          <GoAButton
            onClick={() => {}}
            size="medium"
            variant="danger"
            type="borderless"
          >
            Delete
          </GoAButton>
          <GoAButton onClick={() => {}} size="medium" type="borderless">
            Show
          </GoAButton>
        </td>
      </tr>
    </table>
  );
};

export const IconButtonsTemplate = (args) => {
  return (
    <>
      <div style={{ display: 'flex', gap: '4px' }}>
        <GoAIconButton variant="blah" type="add" />
        <GoAIconButton variant="blah" type="archive" />
        <GoAIconButton variant="blah" type="attach" />
      </div>
      <br />
      <hr />
      <br />
      <div style={{ display: 'flex', gap: '4px' }}>
        <GoAIconButton type="add" />
        <GoAIconButton type="archive" />
        <GoAIconButton type="attach" />
      </div>
    </>
  );
};

## Colors

The colors that exist within the current palette are the all of the gray the 500s and blue-600.

<Canvas>
  <Story name="Gray" args={{ color: 'gray' }}>
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story name="Blue" args={{ color: 'blue' }}>
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story name="Red" args={{ color: 'red' }}>
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story name="Green" args={{ color: 'green' }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Table with tertiary buttons

<Canvas>
  <Story name="List with tertiary button">
    {ListWithTertiaryButtonTemplate.bind({})}
  </Story>
</Canvas>

## Table with borderless buttons

<Canvas>
  <Story name="List with borderless button">
    {ListWithBorderlessButtonTemplate.bind({})}
  </Story>
</Canvas>

## Icon button

<Canvas>
  <Story name="Icon Buttons">{IconButtonsTemplate.bind({})}</Story>
</Canvas>
