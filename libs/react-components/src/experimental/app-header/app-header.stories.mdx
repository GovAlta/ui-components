import GoAAppHeader from './app-header.component';

import {
  Meta,
  Canvas,
  Story,
  ArgsTable,
  Description,
} from '@storybook/addon-docs';
import { action } from '@storybook/addon-actions';

<Meta
  title="Experimental/Web Components/App Header"
  component={GoAAppHeader}
  argTypes={{
  }}
/>

# App  Header

The AppHeader component aims to implement the 'AppHeader' specifications on the Alberta Design System.
The App  Header is displayed in UAT / Test / Syst etc. environments to display an application’s  number and environment label. This header is dismissible to allow it to be hidden during a demo or usability test.

<Canvas>
  <Story
    name="AppHeader"
    height={100}
    args={{ }
  >
    {(args) => (
      <GoAAppHeader />
    )}
  </Story>
</Canvas>

```tsx
<GoAAppHeader />
```

## GoAAppHeader Properties

<Description of={GoAAppHeader} />
<ArgsTable of={GoAAppHeader} />
