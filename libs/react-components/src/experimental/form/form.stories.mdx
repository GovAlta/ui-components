import { GoAFormItem, GoAInput } from '..';
import { useState, useEffect } from 'react';
import {
  Meta,
  Preview,
  Story,
  ArgsTable,
  Description,
} from '@storybook/addon-docs';

import { action } from '@storybook/addon-actions';
import { DoDont, StateDisplay } from '@abgov/shared/storybook-common';

<Meta title="Experimental/Web Components/Form" component={GoAFormItem} />

# GoAForm

React form container is a group of related input controls that allows users to provide data or configure options, so I can quickly layout a form.

## Properties

<ArgsTable of={GoAFormItem} />

## Overview

export const defaultTemplete = (args) => {
  return (
    <GoAFormItem label="First Name" helpText="Do you need help" error="Uh no!">
      <GoAInput
        type="text"
        name="firstName"
        onChange={(name, value) => action('onChange')(name, value)}
      />
    </GoAFormItem>
  );
};

### Default

<Canvas>
  <Story name="Default" height="400px" args={{}}>
    {defaultTemplete.bind()}
  </Story>
</Canvas>
