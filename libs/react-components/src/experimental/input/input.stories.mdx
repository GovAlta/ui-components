import { GoAInput, GoAForm, GoAFormItem } from '../index.ts';
import { useState } from 'react';
import { Do, Dont, State } from '@abgov/shared/storybook-common';
import {
  Meta,
  Preview,
  Story,
  ArgsTable,
  Description,
} from '@storybook/addon-docs/blocks';

<Meta title="Experimental/Components/Input" component={GoAInput} />

# GoAInput

## Overview

export const Template = (args) => {
  const [state, setState] = useState({});
  const onChange = (name, value) => {
    setState({ ...state, [name]: value });
  };
  return (
    <>
      <State
        title="Input Types"
        description="The GoAInput component handles all html input types styled to the GoA UI guidelines"
        state={state}
      >
        <GoAInput {...args} onChange={onChange} name="basic" leadingIcon="personCircle" />
        <GoAInput {...args} onChange={onChange} type="password" name="password" />
        <GoAInput {...args} onChange={onChange} type="date" name="date" />
        <GoAInput {...args} onChange={onChange} type="time" name="time" />
        <GoAInput {...args} onChange={onChange} type="datetime-local" name="datetime" />
      </State>
    </>
  );
};

#### Basic

```ts
interface Props {
  // required
  name: string;
  value: string;
  type: InputType;
  onChange: OnChange;

  // optional
  disabled?: boolean;
  placeholder?: string;
  leadingIcon?: GoAIconType;
}
```

<Canvas>
  <Story name="Basic">{Template.bind({})}</Story>
</Canvas>

```tsx
<GoAInput {...props} />
```

## GoAInput Properties

<Description of={GoAInput} />
<ArgsTable of={GoAInput} />

<Dont description="Don't use placeholder text in text fields" />
<Dont description="Don’t extend inputs longer than 45-75 characters." />
<Dont description="Don't use icons instead of a meaningful label." />
<Do description="Use Text Input if just one line is needed." />
