import {
  GoAInput,
  GoAInputText,
  GoAInputPassword,
  GoAInputDate,
  GoAInputTime,
  GoAInputDateTime,
  GoAInputEmail,
  GoAInputSearch,
  GoAInputUrl,
  GoAInputTel,
  GoAInputFile,
  GoAInputMonth,
  GoAInputNumber,
  GoAInputRange,
  GoAForm,
  GoAFormItem,
} from '../index.ts';
import { useState } from 'react';
import { Do, Dont, State } from '@abgov/shared/storybook-common';
import {
  Meta,
  Preview,
  Story,
  ArgsTable,
  Description,
} from '@storybook/addon-docs/blocks';

<Meta title="Experimental/Components/Input" component={GoAInput} />

# GoAInput

## Overview

export const Template = (args) => {
  const [state, setState] = useState({});
  const onChange = (name, value) => {
    setState({ ...state, [name]: value });
  };
  return (
    <>
      <State
        title="Input Types"
        description="The GoAInput component handles all html input types styled to the GoA UI guidelines"
        state={state}
      >
        <GoAInputText
          {...args}
          onChange={onChange}
          name="basic"
          leadingIcon="person-circle"
        />
        <GoAInputPassword {...args} onChange={onChange} name="password" />
        <GoAInputDate {...args} onChange={onChange} name="date" />
        <GoAInputTime {...args} onChange={onChange} name="time" />
        <GoAInputDateTime {...args} onChange={onChange} name="datetime" />
        <GoAInputEmail {...args} onChange={onChange} name="email" />
        <GoAInputSearch {...args} onChange={onChange} name="search" />
        <GoAInputUrl {...args} onChange={onChange} name="url" />
        <GoAInputTel {...args} onChange={onChange} name="tel" />
        <GoAInputFile {...args} onChange={onChange} name="file" />
        <GoAInputMonth {...args} onChange={onChange} name="month" />
        <GoAInputNumber {...args} onChange={onChange} name="number" />
        <GoAInputRange {...args} onChange={onChange} name="range" />
      </State>
    </>
  );
};

#### Basic

```ts
interface Props {
  // required
  name: string;
  value: string;
  type: InputType;
  onChange: OnChange;

  // optional
  disabled?: boolean;
  placeholder?: string;
  leadingIcon?: GoAIconType;
}
```

<Canvas>
  <Story name="Basic">{Template.bind({})}</Story>
</Canvas>

```tsx
<GoAInput {...props} />
```

## GoAInput Properties

<Description of={GoAInput} />
<ArgsTable of={GoAInput} />

<Dont description="Don't use placeholder text in text fields" />
<Dont description="Don’t extend inputs longer than 45-75 characters." />
<Dont description="Don't use icons instead of a meaningful label." />
<Do description="Use Text Input if just one line is needed." />
