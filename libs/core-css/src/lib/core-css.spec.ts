import { coreCss, componentCss } from './core-css';

describe('coreCss', () => {
  it('coreCss exports', () => {
    expect(coreCss)
  });
});

describe('componentCss', () => {
  it('componentCss exports', () => {
    expect(componentCss)
  });
});
