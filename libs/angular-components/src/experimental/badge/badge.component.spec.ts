import { GoABadgeComponent } from './badge.component';

describe('GoABadgeComponent', () => {
  let component: GoABadgeComponent;

  beforeEach(() => {
    component = new GoABadgeComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
