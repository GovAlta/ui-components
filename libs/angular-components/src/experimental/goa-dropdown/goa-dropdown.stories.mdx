import {
  Meta,
  Canvas,
  Story,
  ArgsTable,
  Description,
} from '@storybook/addon-docs/blocks';
import { moduleMetadata } from '@storybook/angular';
import { action } from '@storybook/addon-actions';
import { WCDropdownComponent } from './goa-dropdown.component';
import { WCDropdownItemComponent } from '../goa-dropdown-item/goa-dropdown-item.component';
import { CUSTOM_ELEMENTS_SCHEMA } from '@angular/core';
// import '@abgov/web-components';

<Meta
  title="Experimental/Components/WCDropdown"
  component={WCDropdownComponent}
  decorators={[
    moduleMetadata({
      declarations: [WCDropdownComponent, WCDropdownItemComponent],
      schemas: [CUSTOM_ELEMENTS_SCHEMA],
    }),
  ]}
  argTypes={{}}
/>

export const Template = (args) => {
  args.changed = (value) => {
    action('value was changed')(value);
  };
  return {
    template: `
    <goa-ng-dropdown name="mylist">
      <goa-ng-dropdown-item value="1" label="Foo" name="mylist"></goa-ng-dropdown-item>
      <goa-ng-dropdown-item value="2" label="Bar" name="mylist"></goa-ng-dropdown-item>
      <goa-ng-dropdown-item value="3" label="Bam" name="mylist"></goa-ng-dropdown-item>
    </goa-ng-dropdown>`,
    props: args,
  };
};

<Story name="Basic">{Template.bind({})}</Story>
