import { DoDont } from '@abgov/shared/storybook-common';
import { GoAHeroBannerComponent, GoAHeroBannerContentComponent, GoAHeroBannerLinkComponent } from './hero-banner.component';

import { Meta, Canvas, Story, ArgsTable, Description } from '@storybook/addon-docs/blocks';
import { moduleMetadata } from '@storybook/angular';
import { action } from '@storybook/addon-actions';
import heroBackground from '../../../../shared/storybook-common/assets/working-in-cold-hero-3_rdax_75.jpg'

<Meta title="Component Library/Components/HeroBanner"
  decorators={[
    moduleMetadata({ declarations: [GoAHeroBannerComponent, GoAHeroBannerContentComponent, GoAHeroBannerLinkComponent] })
  ]}
  component={GoAHeroBannerComponent}
  argTypes={{
    backgroundUrl: { control: "text", description: "Background image" },
  }}
/>

# Header

<Canvas>
  <Story name="With content and link" height={100}
    args={{
      backgroundUrl: heroBackground,
      title: "Upgrading our bitumen",
      content: "Resources are available to help Alberta entrepreneurs and small businesses start, grow and succeed.",
      linkText: "Learn More",
      linkUrl: "http://google.com"
    }}>
      {(args) => {
        return {
          template: `
            <goa-hero-banner [title]="title" [backgroundUrl]="backgroundUrl">
              {{title}}
              <goa-hero-banner-content>{{content}}</goa-hero-banner-content>
              <goa-hero-banner-link [url]="linkUrl">{{linkText}}</goa-hero-banner-link>
            </goa-hero-banner>`,
          props: args
        }
      }}
  </Story>
</Canvas>

<Canvas>
  <Story name="With link only" height={100}
    args={{
      backgroundUrl: "https://www.alberta.ca/goa/design-system/images/working-in-cold-hero-3_rdax_75.jpg",
      title: "Upgrading our bitumen",
      linkText: "Learn More",
      linkUrl: "http://google.com"
    }}>
      {(args) => {
        return {
          template: `
            <goa-hero-banner [title]="title" [backgroundUrl]="backgroundUrl">
              {{title}}
              <goa-hero-banner-link [url]="linkUrl">{{linkText}}</goa-hero-banner-link>
            </goa-hero-banner>`,
          props: args
        }
      }}
  </Story>
</Canvas>

<Canvas>
  <Story name="With header only" height={100}
    args={{
      backgroundUrl: "https://www.alberta.ca/goa/design-system/images/working-in-cold-hero-3_rdax_75.jpg",
      title: "Upgrading our bitumen"
    }}>
      {(args) => {
        return {
          template: `
            <goa-hero-banner [title]="title" [backgroundUrl]="backgroundUrl">
              {{title}}
            </goa-hero-banner>`,
          props: args
        }
      }}
  </Story>
</Canvas>

## Overview

A large image area used to give visual reinforcement of a particular section of content.

## Examples
```html
<goa-hero-banner
    backgroundUrl="https://www.alberta.ca/goa/design-system/images/working-in-cold-hero-3_rdax_75.jpg"
    title="Upgrading our bitumen">
  <goa-hero-banner-content>
    As part of our Made-in-Alberta energy strategy, a partial upgrader will be built in the Industrial Heartland –
    creating more jobs and adding more value to our resources.
  </goa-hero-banner-content>
  <goa-hero-banner-link url="/partial-upgrading-program.aspx">
</goa-hero-banner>
```

## Properties

### GoAHeroBannerComponent
<Description of={GoAHeroBannerComponent} />
<ArgsTable of={GoAHeroBannerComponent} />


### GoAHeroBannerContentComponent
<Description of={GoACardFooterComponent} />

## Usage guidelines

### HeroBanner elements
A hero banner must contain a title

<DoDont
  dos={'Set a title'}
  donts={'Leave title blank'}
/>

A hero banner may contain text content

<DoDont
  dos={'Add some optional text description content'}
  donts={'Include too much content'}
/>
