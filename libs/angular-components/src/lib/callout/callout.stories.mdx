import { DoDont } from '@abgov/shared/storybook-common';
import { GoACalloutComponent } from './callout.component';

import { Meta, Canvas, Story, ArgsTable, Description } from '@storybook/addon-docs/blocks';
import { moduleMetadata } from '@storybook/angular';

<Meta title="Angular/Components/Callout"
  component={GoACalloutComponent}
  argTypes={{
      type: {
        control: 'select',
        table: { //tracked bug in storybook https://github.com/storybookjs/storybook/issues/11983, need table to override display
           defaultValue: { summary: 'information' },
        }
      },
      title: { control: 'text' },
    }
  }
  decorators={[
        moduleMetadata({ declarations: [GoACalloutComponent] }),
    ]
} />

# Callouts
Important changes or facts given a strong visual emphasis so that users take
notice and read the information.

<Canvas>
  <Story name="Variants" height={300}
    args={{
      type: 'information',
      title: 'Callout Title',
    }}
   >
    {(args) => {
      return {
        template: `
          <goa-callout [type]="type" [title]="title">
            Information to the user goes in the content. Information can include <b>markup</b> as desired.
          </goa-callout>`,
        props: args
      }
    }}
  </Story>
</Canvas>



## Overview
Callouts are text boxes that indicate one of the following: important information,
special dates, warning messages, or emergency messages. They occupy the body
layout as a container.

## Examples
```html
<goa-callout type="information" title="Message">Integer et lacus vitae ante faucibus fringilla in in lacus. Nullam bibendum, arcu ut commodo ultrices, justo eros vehicula magna, convallis ultricies velit metus at magna.</goa-callout>
```

## Properties
<Description of={GoACalloutComponent} />
<ArgsTable of={GoACalloutComponent} />

## Usage guidelines
### Within the body
Callout banners grab the attention of the user scanning for important information on
a page, which may be otherwise missed. Callout banners must be relevant to the header
or section it corresponds to.

<DoDont
  dos={'Use a callout banner when the content merits special attention to the topic.'}
  donts={
    'Use too many callout banners. Many competing banners will diminish the effectiveness ' +
    'of individual ones.'
  }
/>

### Light on content
Callout banners should communicate one piece of crucial information pertaining to the topic. In terms of importance within the section, it takes priority over any other paragraph under the header. If there is additional information that is not in the corresponding section, or is on another page, do use a link.

<DoDont
  dos={'Use in-line links when additional information exists.'}
  donts={'Go heavy on content. It is not body copy.'}
/>
