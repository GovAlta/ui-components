import GoAPageLoadIndicatorComponent from './page-load-indicator';
import {
  Meta,
  Preview,
  Story,
  ArgsTable,
  Description,
} from '@storybook/addon-docs';

import { action } from '@storybook/addon-actions';
import { DoDont } from '@abgov/shared/storybook-common';

<Meta
  title="Vue/Components/PageLoader"
  component={GoAPageLoadIndicatorComponent}
  argTypes={{
    type: {
      control: {
        type: 'select',
        options: ['progress', 'infinite'],
      },
    },
    message: {
      control: {
        type: 'text',
      },
    },
    displayType: {
      control: {
        type: 'select',
        options: ['large', 'small'],
      },
    },
  }}
/>

#### Page Load Indicator

<Canvas>
  <Story
    name="PageLoader"
    height={500}
    args={{
      message: 'Loading...',
      type: 'infinite',
      visible: true,
      pagelock: false,
      displayType: 'large',
    }}
  >
    {(args, argTypes) => {
      return {
        render: function (createElement) {
          return createElement(
            GoAPageLoadIndicatorComponent,
            { props: { ...args } },
            null
          );
        },
      };
    }}
  </Story>
</Canvas>

## Examples

```html
<GoAPageLoadIndicatorComponent
  visible="true"
  type="'infinite' || 'progress'"
  loaderType="'large || 'small'"
>
  Loading...
</GoAPageLoadIndicatorComponent>

<div>
  <GoAPageLoadIndicatorComponent
    visible="true"
    type="'infinite' || 'progress'"
    loaderType="'large || 'small'"
    pagelock="false"
  >
    Loading...
  </GoAPageLoadIndicatorComponent>
</div>
```

## Properties

<ArgsTable of={GoAPageLoadIndicatorComponent} />

## Usage guidelines

### PageLoader content

A page loader must contain a label

<DoDont
  dos={'Keep content short and concise'}
  donts={"Place a full sentence in the indicator's label"}
/>
