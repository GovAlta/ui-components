import GoASkeletonImageContent  from './skeleton-image-content';
import GoASkeletonGridColumnContent  from './skeleton-grid-column-content';
import GoASkeletonTitledContent  from './skeleton-titled-content';
import GoASkeletonElement from './skeleton-element';
import GoALoadingCardWithImage from './skeleton-loading-card-with-image';
import GoALoadingCardNoImage from './skeleton-loading-card-no-image';
import { action } from '@storybook/addon-actions';
import { DoDont } from '@abgov/shared/storybook-common';

import {
  Meta,
  Preview,
  Story,
  ArgsTable,
  Description,
} from '@storybook/addon-docs/blocks';

<Meta
  title="Vue/Components/SkeletonElement"
  component={GoASkeletonElement}
  argTypes={{}}
/>

# GoASkeletonElement

A skeleton load element provides visual feedback to users while a content heavy page or element is loading.

## Skeleton Image Content

<Canvas>
  <Story
    name="Skeleton Image Content"
    height={150}
    args={{
      rows: 5,
    }}
  >
    {(args, argTypes) => {
      return {
        components: { GoASkeletonImageContent },
        render: function (createElement) {
          return createElement(GoASkeletonImageContent, {
            props: args,
          });
        },
      };
    }}
  </Story>
</Canvas>

## Content without a title

<Canvas>
  <Story
    name="Content without a title"
    height={150}
    args={{
      rows: 5,
    }}
  >
    {(args, argTypes) => {
      return {
        components: { GoASkeletonGridColumnContent},
        render: function (createElement) {
          return createElement(GoASkeletonGridColumnContent, {
            props: args,
          });
        },
      };
    }}
  </Story>
</Canvas>

## Skeleton Titled Content

<Canvas>
  <Story
    name="Skeleton Titled Content"
    height={150}
    args={{
      rows: 5,
    }}
  >
    {(args, argTypes) => {
      return {
        components: { GoASkeletonTitledContent},
        render: function (createElement) {
          return createElement(GoASkeletonTitledContent, {
            props: args,
          });
        },
      };
    }}
  </Story>
</Canvas>


## Skeleton Card with Image Content

<Canvas>
  <Story
    name="Skeleton Card with Image Content"
    height={150}
    args={{
    }}
  >
    {(args, argTypes) => {
      return {
        components: { GoALoadingCardWithImage},
        render: function (createElement) {
          return createElement(GoALoadingCardWithImage, {
            props: args,
          });
        },
      };
    }}
  </Story>
</Canvas>

## Skeleton Card no Image Content

<Canvas>
  <Story
    name="Skeleton Card no Image Content"
    height={150}
    args={{
    }}
  >
    {(args, argTypes) => {
      return {
        components: { GoALoadingCardNoImage},
        render: function (createElement) {
          return createElement(GoALoadingCardNoImage, {
            props: args,
          });
        },
      };
    }}
  </Story>
</Canvas>

```html
<GoASkeletonImageContent rows="5"></GoASkeletonImageContent>

<GoASkeletonContent rows="5"></GoASkeletonContent>

<GoALoadingCardNoImage />

<GoALoadingCardWithImage />

<GoASkeletonGridColumnContent rows="5"></GoASkeletonGridColumnContent>
```

## Usage guidelines

<DoDont
  dos={
    'Show multiple elements within a larger component as individual information containers'
  }
  donts={
    'Compile several elements that may have different loading durations into one container'
  }
/>

<DoDont
  dos={
    'Include only the most important information containers that make the component recognizable'
  }
  donts={
    'Show every single element as information containers in your skeleton load'
  }
/>

<DoDont
  dos={'Format bones so that they are conducive to a gradual content loading'}
  donts={'Oversimplify the component so that it is not recognizable'}
/>
