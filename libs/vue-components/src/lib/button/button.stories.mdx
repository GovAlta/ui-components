import GoAButton from './button';
import { action } from '@storybook/addon-actions';

import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs/blocks';

<Meta title="Component Library/Page Layout/Button"
  argTypes={{
    buttonType: { control: { type: 'select', options: ['primary', 'secondary', 'tertiary'] } },
    buttonSize: { control: { type: 'select', options: ['small', 'normal'] } },
    tooltip: { control: { type: 'text' } },
    content: { control: { type: 'text', description: 'Click Me!' } },
    onClick: { action: 'You have clicked the button' },
    onMouseDown: { action: 'mouseDown' }
  }}
  component={GoAButton} />

# Button

<Canvas>
  <Story name="Variants" height={150}
    args={{
      buttonType: 'primary',
      buttonSize: 'normal',
      content: "Click Me!",
      tooltip: 'hovering',
    }}>
    {(args, argTypes) => {
      return {
        render: function (createElement) {
          return createElement(
            GoAButton, {
              props: args
            }
          )
        }
      }
    }}
  </Story>
</Canvas>

## Overview

A button to use for a Government of Alberta microsite.  Contains a customizable service name and home link for the microsite.

## Examples
```
<template>
  <div id="app">
    <goa-button
      buttonType="'primary || 'secondary' || 'tertiary'"
      buttonSize="'small' || 'normal'"
      content="Click Me!"
      tooltip="Hovering over"
      v-on:click="() => function()"
    />
  </div>
</template>

<script>
import { GoAButton } from '@abgov/vue-components';

export default {
  name: "App",
  components: {
    'goa-button': GoAButton
  }
};
</script>
```

## Properties

<ArgsTable of={GoAButton} />
