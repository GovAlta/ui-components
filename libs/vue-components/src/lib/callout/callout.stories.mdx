import { DoDont } from '@abgov/shared/storybook-common';
import GoACallout from './callout';
import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs/blocks';

<Meta
  title="Component Library/Components/Callout"
  argTypes={{
    title: { control: { type: 'text' } },
    content: { control: { type: 'text' } },
    calloutType: {
      control: {
        type: 'select',
        options: ['important', 'information', 'event', 'emergency'],
      },
    },
  }}
  component={GoACallout}
/>

# Variants

Important changes or facts given a strong visual emphasis so that users take
notice and read the information.

<Canvas>
  <Story
    name="Variants"
    height={200}
    args={{
      title: 'Callout Title',
      content:
        'Information to the user goes in the content.  Information can include markup as desired.',
      calloutType: 'information',
    }}
  >
    {(args, argTypes) => {
      return {
        components: { GoACallout },
        render: function (createElement) {
          return createElement(GoACallout, {
            props: args,
          });
        },
      };
    }}
  </Story>
</Canvas>

## Overview

Callouts are text boxes that indicate one of the following: important information,
special dates, warning messages, or emergency messages. They occupy the body
layout as a container.

## Examples

```
<template>
  <div id="app">
    <goa-callout title="CalloutTitle" content="Information to the user goes in the content" calloutType="'important || 'information' || 'event' || 'emergency'"/>
  </div>
</template>

<script>
import { GoACallout } from '@abgov/vue-components';

export default {
  name: "App",
  components: {
    'goa-callout': GoACallout
  }
};
</script>
```

or

```
<template>
  <div id="app">
    <goa-callout title="CalloutTitle" content="" calloutType="information"/>
  </div>
</template>

<script>
import { GoACallout } from '@abgov/vue-components';

export default {
  name: "App",
  components: {
    'goa-callout': GoACallout
  }
};
</script>
```

## Properties

<ArgsTable of={GoACallout} />
<Description of={GoACallout} />

## Usage guidelines

### Within the body

Callout banners grab the attention of the user scanning for important information on
a page, which may be otherwise missed. Callout banners must be relevant to the header
or section it corresponds to.

<DoDont
  dos={
    'Use a callout banner when the content merits special attention to the topic.'
  }
  donts={
    'Use too many callout banners. Many competing banners will diminish the effectiveness ' +
    'of individual ones.'
  }
/>

### Light on content

Callout banners should communicate one piece of crucial information pertaining to the topic. In terms of importance within the section, it takes priority over any other paragraph under the header. If there is additional information that is not in the corresponding section, or is on another page, do use a link.

<DoDont
  dos={'Use in-line links when additional information exists.'}
  donts={'Go heavy on content. It is not body copy.'}
/>
