import GoAHeader from './header';
import GoAMicrositeLogo from '../microsite-logo/microsite-logo';

import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs';

<Meta
  title="Vue/Page Layout/Header"
  argTypes={{
    serviceName: { control: { type: 'text' } },
    serviceHome: { control: { type: 'text' } },
    serviceLevel: {
      control: 'text',
      description: 'Level of the service [alpha | beta | live]',
    },
  }}
  component={GoAHeader}
/>

# Header

<Canvas>
  <Story
    name="Header"
    height={150}
    args={{
      serviceName: 'Digital Service Name (microsite)',
      serviceHome: '/',
      serviceLevel: 'beta',
    }}
  >
    {(args, argTypes) => {
      return {
        render: function (createElement) {
          return createElement(GoAHeader, {
            components: { GoAMicrositeLogo },
            props: args,
          });
        },
      };
    }}
  </Story>
</Canvas>

## Overview

A header to use for a Government of Alberta microsite. Contains a customizable service name and home link for the microsite.

## Examples

```
<template>
  <div id="app">
    <goa-header serviceName="Sample usage example" serviceHome="/" serviceLevel="alpha" />
  </div>
</template>

<script>
import { GoAHeader } from '@abgov/vue-components';

export default {
  name: "App",
  components: {
    'goa-header': GoAHeader
  }
};
</script>
```

## Properties

<ArgsTable of={GoAHeader} />
