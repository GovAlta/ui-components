
import { DoDont } from '@abgov/shared/storybook-common';
import GoAHeader from './header';
import GoAMicrositeLogo from '../microsite-logo/microsite-logo';
import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs/blocks'; 

<Meta title="Component Library/Page Layout/Header" 
  argTypes={{
    serviceName: { control: { type: 'text' } },
    serviceHome: { control: { type: 'text' } },
  }}
  component={GoAHeader} />

# Header

<!-- 
  This syntax isn't the greatest... It supposedly works with template and props properties, but I could not get it to work.
<Canvas>
  <Story name="Micro Site Logo" height={100}>
    {{
      components: { GoAHeader },
      template: `<goa-header :serviceName="serviceName" :serviceHome="serviceHome" />`,
      props: {
        serviceName: {
          type: String,
          default: text('serviceName', 'Microsite name')
        },
        serviceHome: {
          type: String,
          default: text('serviceHome', '/')
        }
      }  
    }}
  </Story>
</Canvas>
-->
<Canvas>
  <Story name="Header" height={150}
    args={{
      serviceName: 'Microsite name',
      serviceHome: '/'
    }}>
    {(args, argTypes) => {
      return {
        render: function (createElement) {
          return createElement(
            GoAHeader, {
              components: { GoAMicrositeLogo },
              props: args
            }
          )
        }
      }
    }}
  </Story>
</Canvas>

## Overview

A header to use for a Government of Alberta microsite.  Contains a customizable service name and home link for the microsite.

## Examples
```
<template>
  <div id="app">
    <goa-header serviceName="Sample usage example" serviceHome="/" />
  </div>
</template>

<script>
import { GoAHeader } from '@abgov/vue-components';

export default {
  name: "App",
  components: {
    'goa-header': GoAHeader
  }
};
</script>
```

## Properties

<ArgsTable of={GoAHeader} />