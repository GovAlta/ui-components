
import { DoDont } from '@abgov/shared/storybook-common';
import { GoAMicrositeLogo } from './microsite-logo';
import { Meta, Preview, Story, Props } from '@storybook/addon-docs/blocks'; 
import { text, withKnobs } from '@storybook/addon-knobs';

<Meta title="Design System/Basic Elements/Header" 
  decorators={[
    
  ]}
  component={GoAMicrositeLogo} />

# Header

<!-- 
  This syntax isn't the greatest... It supposedly works with template and props properties, but I could not get it to work.
<Preview>
  <Story name="Micro Site Logo" height={100}>
    {{
      components: { GoAMicrositeLogo },
      template: `<goa-microsite-logo :serviceName="serviceName" :serviceHome="serviceHome" />`,
      props: {
        serviceName: {
          type: String,
          default: text('serviceName', 'Microsite name')
        },
        serviceHome: {
          type: String,
          default: text('serviceHome', '/')
        }
      }  
    }}
  </Story>
</Preview>
-->
<Preview>
  <Story name="Micro Site Logo" height={100}>
    {{
      components: { GoAMicrositeLogo },
      render: function (createElement) {
        return createElement(
          GoAMicrositeLogo, {
            props: {
              'serviceName': text('serviceName', 'Digital Service Name (microsite)'),
              'serviceHome': text('serviceHome', 'http://alberta.ca')            
            }
          }
        )
      },
    }}
  </Story>
</Preview>

## Overview

A logo to use for a Government of Alberta microsite.  Contains a customizable service name and home link for the microsite.

## Examples
```
<template>
  <div id="app">
    <goa-microsite-logo serviceName="Sample usage example" serviceHome="/" />
  </div>
</template>

<script>
import { GoAMicrositeLogo } from '@abgov/vue-components';

export default {
  name: "App",
  components: {
    'goa-microsite-logo': GoAMicrositeLogo
  }
};
</script>
```

## Properties

<Props of={GoAMicrositeLogo} />