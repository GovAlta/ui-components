import { DoDont } from '@abgov/shared/storybook-common';
import GoAAppVersionHeader from './app-version-header';
import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs/blocks';

<Meta
  title="Experimental/Components/AppVersionHeader"
  argTypes={{
    isProdEnvironment: {
      control: 'boolean',
      table: {
        //tracked bug in storybook https://github.com/storybookjs/storybook/issues/11983, need table to override display
        defaultValue: { summary: false },
        type: { summary: 'boolean' },
      },
    },
    environment: { control: 'text' },
    version: { control: 'text' },
  }}
  component={GoAAppVersionHeader}
/>

# App Version Header

The AppVersionHeader component aims to implement the 'AppVersionHeader' specifications on the Alberta Design System.
The App Version Header is displayed in UAT / Test / Syst etc. environments to display an application’s version number and environment label. This header is dismissible to allow it to be hidden during a demo or usability test.

<Canvas>
  <Story
    name="AppVersionHeader"
    height={100}
    args={{
      isProdEnvironment: false,
      environment: 'test',
      version: '1.0',
    }}
  >
    {(args, argTypes) => {
      return {
        components: { GoAAppVersionHeader },
        render: function (createElement) {
          return createElement(GoAAppVersionHeader, {
            props: args,
          });
        },
      };
    }}
  </Story>
</Canvas>

## Examples

```
<template>
  <div id="app">
    <goa-app-verion-header
        isProdEnvironment="true"
        environment="dev"
        version="1.0"
    />
  </div>
</template>

<script>
import { GoAAppVersionHeader } from '@abgov/vue-components';

export default {
  name: "App",
  components: {
    'goa-app-verion-header': GoAAppVersionHeader
  }
};
</script>
```

## GoAAppVersionHeader Properties

<Description of={GoAAppVersionHeader} />
<ArgsTable of={GoAAppVersionHeader} />
