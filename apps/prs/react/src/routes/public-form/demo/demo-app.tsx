import { GoabText } from "@abgov/react-components";

/**
 * Public form demo (the real flow).
 *
 * Unlike examples/, which showcases each UI pattern in isolation, this is one
 * coherent service built from the shared public-form pieces (PublicFormLayout,
 * FormSet, ErrorSummary, validation, FormSummary, ResultsPage). It follows the
 * Figma "Pattern - Public Form v1" diagram, starting with Section 1A (Screener).
 *
 * Blank for now: the route and folder exist so the flow can be built in place.
 */
export function DemoApp() {
  return (
    <GoabText tag="h1" mt="none" mb="none">
      Demo
    </GoabText>
  );
}
